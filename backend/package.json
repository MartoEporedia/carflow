{
  "name": "carflow-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "wrangler d1 migrations apply carflow-db --local",
    "db:migrate:prod": "wrangler d1 migrations apply carflow-db --remote"
  },
  "dependencies": {
    "@better-auth/core": "^1.2.0",
    "better-auth": "^1.2.0",
    "hono": "^4.6.0",
    "@hono/zod-validator": "^0.4.1",
    "drizzle-orm": "^0.36.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241112.0",
    "drizzle-kit": "^0.28.0",
    "typescript": "^5.6.3",
    "wrangler": "^3.87.0"
  }
}
